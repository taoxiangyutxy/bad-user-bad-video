<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ttt.one.admin.dao.SysUserMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.ttt.one.admin.entity.SysUser" id="sysUserMap">
        <result property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="salt" column="salt"/>
        <result property="email" column="email"/>
        <result property="mobile" column="mobile"/>
        <result property="status" column="status"/>
        <result property="createUserId" column="create_user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="updateUserId" column="update_user_id"/>
    </resultMap>

    <!-- 根据用户ID查询角色列表，支持动态条件 -->
    <select id="selectRolesByUserId" resultType="com.ttt.one.admin.entity.SysRole">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY r.create_time DESC
    </select>

    <!-- 根据条件查询用户列表，支持多种动态条件 -->
    <select id="selectUsersByCondition" resultType="com.ttt.one.admin.entity.SysUser">
        SELECT * FROM sys_user
        <where>
            <if test="condition.username != null and condition.username != ''">
                <bind name="usernamePattern" value="'%' + condition.username + '%'"/>
                AND username LIKE #{usernamePattern}
            </if>
            <if test="condition.email != null and condition.email != ''">
                AND email LIKE CONCAT('%', #{condition.email}, '%')
            </if>
            <if test="condition.mobile != null and condition.mobile != ''">
                AND mobile LIKE CONCAT('%', #{condition.mobile}, '%')
            </if>
            <if test="condition.status != null">
                AND status = #{condition.status}
            </if>
            <if test="condition.startTime != null">
                AND create_time >= #{condition.startTime}
            </if>
            <if test="condition.endTime != null">
                AND create_time &lt;= #{condition.endTime}
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="condition.pageSize != null and condition.pageSize > 0">
            LIMIT #{condition.pageSize}
            <if test="condition.offset != null and condition.offset >= 0">
                OFFSET #{condition.offset}
            </if>
        </if>
    </select>

    <!-- 统计用户数量 -->
    <select id="countUsersByCondition" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_user
        <where>
            <if test="condition.username != null and condition.username != ''">
                AND username LIKE CONCAT('%', #{condition.username}, '%')
            </if>
            <if test="condition.email != null and condition.email != ''">
                AND email LIKE CONCAT('%', #{condition.email}, '%')
            </if>
            <if test="condition.mobile != null and condition.mobile != ''">
                AND mobile LIKE CONCAT('%', #{condition.mobile}, '%')
            </if>
            <if test="condition.status != null">
                AND status = #{condition.status}
            </if>
            <if test="condition.startTime != null">
                AND create_time >= #{condition.startTime}
            </if>
            <if test="condition.endTime != null">
                AND create_time &lt;= #{condition.endTime}
            </if>
        </where>
    </select>
    <select id="findByUsername" resultType="com.ttt.one.admin.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username}
        AND status = 1 LIMIT 1
    </select>
    <select id="getUsersByRoleId" resultType="com.ttt.one.admin.entity.SysUser">
        SELECT * FROM sys_user u
                          LEFT JOIN sys_user_role ur ON ur.user_id = u.user_id
        WHERE ur.role_id = #{roleId}
    </select>
</mapper>